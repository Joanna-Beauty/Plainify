# 安全政策

## 支持范围

当前只维护最新公开版本。修复安全问题时，维护者会在 `main` 分支和后续 Release 中发布修复。

## 报告安全问题

请不要用公开 Issue 报告尚未修复的漏洞。请通过仓库的
[Private vulnerability reporting](https://github.com/Joanna-Beauty/Plainify/security/advisories/new)
私密提交问题，并提供以下信息：

- 受影响的版本、系统和浏览器。
- 可以稳定复现问题的最小步骤。
- 可能造成的影响，以及你已经验证过的缓解方式。
- 不包含真实 API Key、账号密码或其他个人信息的日志和截图。

如果 API Key 可能已经泄露，请先到对应模型服务商撤销并更换 Key，再提交脱敏后的报告。

## 安全边界

- 本机服务只应监听 `127.0.0.1`，不得暴露到局域网或公网。
- API Key 只允许保存在权限为 `0600` 的 `.env.local` 中，不得进入网页存储、扩展、日志或 Git。
- 请只从本仓库的 Release 或源码下载项目，并在安装前核对发布说明。
