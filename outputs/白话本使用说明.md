# 白话本

## 打开应用

开发服务器地址：`http://127.0.0.1:5173/`

以后重新运行：

```bash
npm install
npm run dev -- --port 5173
```

## 使用大模型

1. 打开项目根目录的 `.env.local`。
2. 在 `DEEPSEEK_API_KEY=` 后粘贴自己的 DeepSeek API Key。
3. 重新运行 `npm run dev -- --port 5173`。
4. 打开网站“设置”，从下拉列表选择 DeepSeek 模型，再点击“测试 DeepSeek”。

DeepSeek 的接口地址已经内置，不需要输入，也不能在设置页修改。API Key 只保存在本机 `.env.local`，由绑定在 `127.0.0.1:8787` 的后端读取；网站和插件都拿不到 Key，也不会把它写进浏览器存储。不要把真实 Key 发到聊天、提交到 Git 或打进扩展压缩包。

## 安装浏览器插件

1. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择项目里的 `extension/` 目录。
5. 回到白话本设置页，选择网页悬停解释方式，再点击“同步术语与插件设置”。

安装后，在普通网页中选中文字，原位置旁会出现“用大白话解释”按钮。点击后先显示大白话解释和生活化类比，此时术语尚未保存：可以选择“先不添加”，也可以选择“加入白话本”。只有确认加入后，术语才会同步到网站并显示黄色高亮。

点击浏览器工具栏里的白话本图标，可以手动输入术语并点击“生成解释”。查询不会直接收录；最新查询在弹窗关闭后仍会保留，确认“加入白话本”后才会写入术语库。点击“清空”后可以查询新词。

在网站设置页的“网页悬停显示”中，可以选择只显示“大白话解释”、只显示“生活化类比”或“全部展示”。保存设置后，鼠标悬停在网页黄色高亮词上会按该方式显示。右键菜单仍保留直接解释并收录的快捷方式。插件调用大模型时需要本机后端保持运行。

## 数据位置

网页数据保存在浏览器 `localStorage`，扩展数据保存在 `chrome.storage.local`，API Key 单独保存在 `.env.local`。当前版本没有账号系统和云同步，更换浏览器或清理浏览器数据前需要自行备份术语。
