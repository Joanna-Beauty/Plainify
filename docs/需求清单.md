# 加简大白话 · Plainify 需求清单

更新时间：2026-08-29

## 记录规则

- 一个需求只解决一个问题。
- 新想法默认先进入需求清单，不直接进入开发。
- 开发前需要确定优先级、依赖关系和验收标准。
- 状态分为：待讨论、已确定、待开发、开发中、已完成、暂不做。
- 优先级分为：P0 为阻塞当前版本发布的需求，P1 为发布前应完成的质量与反馈能力，P2 为后续迭代。

## 产品路线

### 第一阶段：本地单用户版

当前版本优先满足个人本地使用：用户可以从 GitHub 下载项目，在自己的电脑上运行网站、后端和浏览器扩展。

- 不要求注册或登录账号。
- 术语与复习数据保存在本机。
- DeepSeek 和 OpenAI API Key 可以在前端设置页填写。
- 前端提交的 API Key 由本机后端写入受保护且不会提交到 Git 的配置文件。
- 浏览器扩展和网页通过本机后端调用当前选定的模型提供方。

### 第二阶段：云账号版

产品成熟后增加账号体系和云端数据同步。

- 用户在不同电脑登录同一账号后看到同一份数据。
- 任意设备新增或修改数据后，其他设备自动获得更新。
- API Key 与账号绑定并安全保存在云端后端，不需要每台设备重复配置。
- 云端具体使用自建后端、Supabase，还是 iCloud/网盘方案，暂未确定。

## 当前高优需求

| 优先级 | 范围 | 状态 | 验收记录 |
| --- | --- | --- | --- |
| P0 | `STATE-01` | 已完成 | `docs/共享状态契约.md` 已明确状态所有权、术语生命周期、同步优先级、跨模块影响和待收敛问题 |
| P0 | `STATE-02`～`STATE-06` | 已完成 | 术语字段按领域记录更新时间，归档与掌握状态拆分，复习队列固定，双端冲突和模型选择均已对齐 |
| P0 | `FLOW-01` | 已完成 | 自动化核心流程覆盖模型选择、收录、解释、分组、复习、归档、重启同步和恢复；浏览器完成 5 词复习无跳词 |
| P0 | `TEST-01` | 已完成 | `npm run test:release` 已接入归档、分组、复习、同步、服务恢复和核心流程测试 |
| P0 | `DIST-01`～`DIST-03` | 已完成 | 隔离目录执行 `npm ci`、生产构建、网站启动和后端健康检查均通过；根目录 README 和配置模板已齐全 |
| P0 | `KEY-L01`～`KEY-L06` | 已完成 | `npm run test:release` 覆盖填写、验证、持久化、权限、非明文回显、替换、重启恢复和删除 |
| P1 | `WEB-07` | 已完成 | 关闭与归档按钮均提供明确的悬停提示 |
| P1 | `CONTACT-01`、`CONTACT-02`、`CONTACT-04` | 已完成 | 联系页展示可复制的邮箱、微信与社媒账号，并提供可访问的飞书需求登记表 |
| P1 | `STYLE-07` | 已完成 | 1280×720 桌面视口与 390×844 移动视口的首页、设置及联系页无横向溢出、遮挡或文字截断 |

## 功能闭环与共享状态

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| STATE-01 | 建立本地版共享状态契约 | 已完成 | 状态事实来源、术语字段不变量、生命周期、同步方向及跨模块影响均有唯一文档说明 |
| STATE-02 | 统一术语标准化和状态变更入口 | 已完成 | 网站和扩展统一名称、解释状态、时间戳与字段领域规则，网站业务操作通过共享数据函数更新 |
| STATE-03 | 拆清复习掌握状态与归档状态 | 已完成 | `mastered` 只记录最近复习结果，归档和恢复均保持原值 |
| STATE-04 | 固定单轮复习队列 | 已完成 | 一轮开始后术语顺序固定，连续完成 5 个术语无跳过或重复；再来一轮才按最新结果重新选词 |
| STATE-05 | 明确网站与扩展的冲突处理 | 已完成 | 内容、分组、复习和归档分别按更新时间合并；主动清空字段和删除墓碑均有专项测试 |
| STATE-06 | 对齐模型选择状态 | 已完成 | 设置页以后端激活配置回写网站，并通过统一设置同步到已连接扩展 |
| FLOW-01 | 建立本地版核心端到端闭环 | 已完成 | 自动化覆盖模型选择、收录、解释、分组、复习、归档、序列化重启、扩展合并和恢复 |
| TEST-01 | 扩大发布测试门禁 | 已完成 | `npm run test:release` 覆盖模型、归档、分组、复习、同步、后端恢复、核心流程和发布契约 |

共享状态的当前事实、目标规则和待确认问题见 `docs/共享状态契约.md`。

## 网页解释交互

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| WEB-01 | 将当前问号标识改为小灯泡图标 | 已完成 | 网页术语交互入口显示灯泡图标 |
| WEB-02 | 点击高亮术语后固定解释栏 | 已完成 | 鼠标移开后解释栏仍然显示 |
| WEB-03 | 固定解释栏支持关闭 | 已完成 | 点击关闭按钮后解释栏消失 |
| WEB-04 | 固定解释栏提供归档操作 | 已完成 | 点击归档后，术语真实进入网站归档库，刷新网站和网页后归档状态仍然保留 |
| WEB-07 | 固定解释栏按钮提供悬停功能提示 | 已完成 | 鼠标停在关闭、归档等按钮上时，显示“关闭解释栏”“归档术语”等明确文字提示 |

## 联系与需求反馈

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| CONTACT-01 | 增加“联系我 / 提需求”入口 | 已完成 | 用户可以从网站固定且容易找到的位置进入联系与反馈页面 |
| CONTACT-02 | 展示邮箱和社媒账号 | 已完成 | 页面展示可点击或可复制的邮箱及社媒账号，并清楚标注各渠道名称 |
| CONTACT-03 | 展示微信二维码 | 暂不做 | 当前不展示微信二维码，保留微信号 `around_9` 及复制操作 |
| CONTACT-04 | 接入飞书需求收集表格 | 已完成 | 用户点击“需求登记表”后可以在新标签页打开指定飞书表单并提交需求 |

社媒当前提供可复制账号；后续获得 B 站和小红书的固定主页链接后，可再增加直达操作。

## 数据统计

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| DATA-01 | 增加使用数据埋点，统计使用用户数 | 待讨论 | 按确定的用户去重口径和时间周期查看使用用户数，重复使用不会被重复计数 |

实现前需确定：“使用用户”的触发事件，无账号用户的匿名去重方式，统计周期（累计、日活或月活），数据上报与查看方案，以及隐私告知、用户同意和退出机制。

## 术语归档

归档用于暂时退出学习流程，不等同于删除：术语内容、来源、分组和复习记录仍需保留，并且可以恢复到术语库。

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| TERM-01 | 归档术语但不删除术语数据 | 已完成 | 归档后解释、类比、来源、分组和复习记录保持不变 |
| TERM-02 | 给熟悉的术语打钩即归档 | 已完成 | 点击术语卡片的勾选按钮后，术语进入归档并从首页消失 |
| TERM-03 | 归档状态保存在本机 | 已完成 | 刷新网站、重启浏览器或重启本机服务后归档状态不丢失 |
| WEB-05 | 已归档术语不再显示网页高亮 | 已完成 | 归档后所有已打开网页和新打开网页都不再高亮该术语 |
| WEB-06 | 从网页固定解释栏归档术语后更新当前界面 | 已完成 | 归档后固定解释栏立即关闭，当前网页中的同名高亮立即消失 |
| LIB-01 | 加简大白话支持归档术语 | 已完成 | 用户给术语打钩后，该术语从首页移入归档 |
| LIB-02 | 加简大白话支持取消归档 | 已完成 | 归档视图的术语可以恢复到术语库 |
| LIB-03 | 术语详情提供归档和恢复入口 | 已完成 | 熟悉中术语显示归档操作，已归档术语显示恢复操作 |
| LIB-04 | 分组组件下方提供归档入口 | 已完成 | 首页不显示归档术语；点击归档组件后单独查看全部归档术语 |
| LIB-05 | 搜索结果遵循当前归档筛选范围 | 已完成 | 首页搜索只查未归档术语，归档视图搜索只查已归档术语 |
| LIB-06 | 已归档术语支持永久删除 | 已完成 | 用户可以从归档列表或详情中删除术语 |
| REVIEW-01 | 复习排除已归档术语 | 已完成 | 快速复习不会抽取已归档术语 |
| GROUP-01 | 归档记录术语当时的分组 | 已完成 | 未分组术语归入归档的“未分组”，已分组术语归入归档的对应分组 |
| GROUP-02 | 已归档术语不参与分组与统计 | 已完成 | 首页分组数量、增量分组和全量重分组均排除归档术语 |
| GROUP-03 | 恢复时检查原分组是否仍存在 | 已完成 | 原分组存在则恢复到原分组；不存在则恢复到未分组，再归档时进入归档的未分组 |
| EXT-01 | 网站与扩展同步术语归档状态 | 已完成 | 网站归档或恢复后，扩展同步更新且不会因旧数据覆盖新状态 |
| EXT-02 | 扩展收到归档状态后移除网页高亮 | 已完成 | 同步完成后无需刷新网页，对应黄色高亮自动消失 |

## 品牌与视觉风格

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| BRAND-01 | 确定产品正式名称 | 已完成 | 正式名称为“加简大白话 · Plainify｜你的个人术语库” |
| BRAND-02 | 设计产品 Logo | 待讨论 | 产出可用于网页的标准 Logo |
| BRAND-03 | 制作 Logo 形式变体 | 待讨论 | 提供完整 Logo、图标版和 favicon 版本 |
| BRAND-04 | 主界面展示品牌标识 | 已完成 | 桌面端侧栏和移动端顶部固定显示产品名称和图标 |
| STYLE-01 | 确定前端视觉方向 | 待讨论 | 明确风格关键词、参考和需要避免的风格 |
| STYLE-02 | 统一全局颜色体系 | 待开发 | 页面使用统一的品牌色和功能色 |
| STYLE-03 | 统一全局字体体系 | 待开发 | 标题、正文、按钮和辅助文字层级统一 |
| STYLE-04 | 统一全局间距与尺寸 | 待开发 | 页面布局和控件尺寸遵循统一规则 |
| STYLE-05 | 统一主要控件样式 | 待开发 | 按钮、输入框、菜单、提示层和抽屉视觉一致 |
| STYLE-06 | 统一网站与扩展视觉 | 待开发 | 网站、插件弹窗和网页解释栏使用同一品牌风格 |
| STYLE-07 | 验证响应式界面 | 已完成 | 桌面端和移动端没有溢出、遮挡或文字截断 |

## 本地版发布与 API Key

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| DIST-01 | 项目可以从 GitHub 下载 | 已完成 | 用户下载项目后可以在本机安装运行 |
| DIST-02 | 提供本地安装说明 | 已完成 | 根目录 README 覆盖依赖安装、网站与后端启动、macOS 常驻服务和 Chrome/Edge 扩展安装 |
| DIST-03 | 提供必要的配置模板 | 已完成 | 仓库包含 `.env.example` 占位配置，`.env.local` 被 Git 忽略且不进入扩展包 |
| KEY-L01 | 前端设置页支持填写 API Key | 已完成 | 用户无需手动打开配置文件即可填写 Key |
| KEY-L02 | 本机后端持久化保存 API Key | 已完成 | Key 保存至 `.env.local`，隔离后端重启后可恢复提供方与模型配置 |
| KEY-L03 | API Key 配置文件不进入 Git | 已完成 | `.env.local` 权限为 `0600`，Git 状态和扩展发布包中不包含该文件 |
| KEY-L04 | 前端不回显完整 API Key | 已完成 | 再次打开设置页时密码输入框为空，只显示“已配置，输入新值可替换” |
| KEY-L05 | 支持更换本机 API Key | 已完成 | 新 Key 验证成功后原子替换旧 Key，旧值不再保留 |
| KEY-L06 | 支持删除本机 API Key | 已完成 | 删除后返回 `backend_not_configured`，且不再向对应模型提供方发起请求 |

### 设置页模型配置交互

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| MODEL-01 | 设置默认进入通用设置 | 已完成 | 每次打开设置时先展示通用设置，用户点击“模型”后才进入模型配置 |
| MODEL-02 | 仅提供 DeepSeek 和 OpenAI | 已完成 | 提供方选择中没有自定义提供方或其他厂商 |
| MODEL-03 | 未配置模型时只展示添加入口 | 已完成 | 模型页没有已配置提供方时，仅展示“添加提供方”按钮 |
| MODEL-04 | 添加提供方时默认选择首个未配置厂商 | 已完成 | 打开添加面板后使用下拉列表选择提供方，API Key 为必填项；切换厂商会重置为该厂商的官方地址和默认模型 |
| MODEL-05 | 自定义设置默认收起 | 已完成 | 添加或编辑提供方时均先收起 API 地址和模型目录，用户主动点击后展开 |
| MODEL-06 | 官方 API 地址支持自定义覆盖 | 已完成 | 默认使用所选提供方的官方 API 地址；用户填写自定义地址后，模型发现、连接验证和后续调用均优先使用自定义地址 |
| MODEL-07 | 从厂商接口获取模型列表 | 已完成 | 未填写 Key 也可点击获取官方预置清单；填写临时 Key 后从当前 API 地址动态获取并过滤可用模型 |
| MODEL-08 | 模型名称不允许手动输入 | 已完成 | 用户只能从厂商返回的模型列表选择，避免因手输错误导致调用失败 |
| MODEL-09 | 恢复适配器默认模型 | 已完成 | 恢复默认会清空尚未保存的自定义模型目录，并重新使用该适配器的默认模型 |
| MODEL-10 | 取消或切换页签会回滚编辑 | 已完成 | 点击取消或切换设置页签后退出未保存的提供方编辑，不修改本机配置 |
| MODEL-11 | 已配置提供方显示状态卡片 | 已完成 | 卡片显示提供方名称、配置状态和编辑入口；编辑时仍可从下方添加其他未配置提供方 |
| MODEL-12 | 获取并保存模型配置具有明确反馈 | 已完成 | 获取时显示加载状态；保存或切换时优先用自定义地址验证选定模型并展示结果；空 Key 不能新增提供方，未指定模型时使用官方默认模型 |

## 云账号与跨设备同步

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| ACCOUNT-01 | 用户通过账号识别个人资料库 | 已确定 | 同一账号在不同设备访问同一资料库 |
| SYNC-01 | A 电脑的新增数据同步到 B 电脑 | 已确定 | A 新增术语后 B 可以自动看到 |
| SYNC-02 | B 电脑的新增数据同步到 A 电脑 | 已确定 | B 新增术语后 A 可以自动看到 |
| SYNC-03 | 术语修改在设备间同步 | 已确定 | 解释、类比、分组和状态修改保持一致 |
| SYNC-04 | 新设备恢复已有历史数据 | 已确定 | 新电脑登录后自动获得此前数据 |
| SYNC-05 | 当前本地数据迁移到账号 | 待设计 | 升级云版本时不会丢失现有本地记录 |
| SYNC-06 | 处理同时修改产生的数据冲突 | 待设计 | 两台设备修改同一术语时不会静默覆盖有效内容 |
| SYNC-07 | 显示同步状态 | 待设计 | 用户能看到同步中、已同步、失败和离线状态 |
| SYNC-08 | 支持离线修改后继续同步 | 待设计 | 恢复网络后自动上传离线期间的修改 |
| SYNC-09 | 隔离不同账号的数据 | 已确定 | 其他账号无法读取当前用户的数据 |

## 云账号版 API Key

| 编号 | 原子需求 | 状态 | 验收标准 |
| --- | --- | --- | --- |
| KEY-C01 | API Key 与用户账号绑定 | 已确定 | 不同账号使用各自的 Key |
| KEY-C02 | API Key 配置跨设备生效 | 已确定 | A 配置后 B 登录同一账号即可调用模型 |
| KEY-C03 | 云端后端加密保存 API Key | 待设计 | 数据库或密钥服务中不保存明文 Key |
| KEY-C04 | 云端后端代理 DeepSeek 请求 | 已确定 | 浏览器和扩展无法读取完整 Key |
| KEY-C05 | 支持更换账号 API Key | 已确定 | 新 Key 生效后所有设备统一使用新配置 |
| KEY-C06 | 支持删除账号 API Key | 已确定 | 删除后所有设备停止使用该 Key |
| KEY-C07 | 日志不记录完整 API Key | 已确定 | 请求日志和错误信息不包含完整 Key |

## 待讨论事项

1. 云端使用自建后端、Supabase，还是 iCloud/网盘同步文件。
2. 云账号使用邮箱、手机号、第三方登录，还是其他登录方式。
