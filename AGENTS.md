# 加简大白话 · Plainify Agent 指南

本文件适用于在此仓库中工作的编码 Agent。面向用户的安装和使用说明以
`README.md` 为准；产品范围、优先级和验收标准以 `docs/需求清单.md` 为准。

## 项目结构

- `src/`：React + Vite 网页应用。
- `server/`：只监听本机地址的 Node.js 后端，以及 macOS LaunchAgent 安装脚本。
- `extension/`：Chrome/Edge 扩展源码，是浏览器插件的唯一源码目录。
- `work/`：发布契约、集成和专项回归测试。
- `docs/需求清单.md`：需求状态和验收标准的事实来源。
- `dist/`：Vite 构建产物，不要手工修改。
- `outputs/`：可交付说明、截图和扩展压缩包；只有任务明确涉及发布物时才更新。

## 修改原则

1. 开始前运行 `git status --short`，识别并保留用户已有的未提交修改。
2. 先阅读相关代码和需求验收标准，再进行范围最小的实现。
3. 不顺手重构无关模块，不覆盖、不回退无法确认来源的改动。
4. 优先复用现有组件、数据结构、服务接口和测试方式。
5. 完成或调整需求时，同步更新 `docs/需求清单.md` 的状态、验收标准或验收记录。
6. 用户可见行为发生变化时，同步检查根目录和扩展的 README 是否需要更新。

## 数据与安全

- `.env.local` 包含本机密钥，禁止提交、输出、复制到测试夹具或打入扩展包。
- `.env.example` 只能保存占位符和非敏感默认值。
- API Key 只允许由本机后端读取和持久化；网页、扩展、日志和 API 响应不得获得完整值。
- 修改密钥持久化时，保持文件权限 `0600`、非明文回显、验证成功后原子替换等约束。
- 不读取或展示真实 Key 来完成测试；优先使用临时目录、测试值和模拟服务。

## 模型配置约束

- 提供方、官方 API 地址和默认模型以 `src/data/modelProviders.js` 的配置为准。
- 未填写自定义 API 地址时使用提供方官方地址；填写后，模型发现、连接验证和调用均优先使用自定义地址。
- 新增提供方必须填写 API Key；获取官方预置模型清单可以不填写 Key。
- 用户未选择或未获取模型时，使用该提供方的官方默认模型。
- 前端交互、后端校验和实际调用必须保持相同的提供方、地址及模型解析规则。

## 浏览器扩展与本机服务

- 扩展源码改动只在 `extension/` 中完成；不要直接修改 `outputs/extension/`。
- 扩展改动需检查 `manifest.json` 权限、网页内容脚本、弹窗和网站同步协议是否仍一致。
- 后端或启动逻辑改动后，按需运行 `npm run service:install` 应用到 macOS 常驻服务，并用
  `npm run service:status` 检查状态。
- 不要把个人机器的绝对项目路径写入源码；安装脚本应从自身位置解析仓库根目录。

## 验证

交付代码前至少运行：

```bash
npm run lint
npm run build
npm run test:release
git diff --check
```

根据改动范围补充运行 `work/` 中的专项测试。前端视觉改动还需在桌面和移动视口检查
横向溢出、遮挡、文字截断及关键交互；扩展改动需在 Chrome 或 Edge 中重新加载后验证。
如任何检查因环境限制未运行，交付时明确说明未验证项和原因。
